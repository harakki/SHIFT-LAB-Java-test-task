spring:
  application:
    name: shift-lab
  mvc:
    problem-details:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
  docker:
    compose:
      file: src/main/docker/compose.yaml
